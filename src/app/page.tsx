// Default landing page — Clockii marketing site.
// The full implementation lives in `./clockii/page.tsx`; this file re-exports it
// so the same component is served at `/` and at `/clockii`.
//
// The previous Shipcube internal landing is preserved next to this file as
// `page.tsx.shipcube-backup` if you ever need to restore it.

export { default } from './clockii/page';
